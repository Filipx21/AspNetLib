@model InfrastructureAndState.Models.Counters
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style>
        div {
            margin-bottom: 50px;
        }
    </style>
</head>
<body>
    <div>
        <p>Aktualna godzina: @DateTime.Now.ToLongTimeString()</p>
        <p>@Html.Action("GetTime", true)</p>
    </div>

    @using (Html.BeginForm("PassTempData", "Home"))
    {
        <div>
            Wpisz parametr do przekazania między żądaniami

            <input type="text" name="paramtopass" />
            <input value="Wyślij" type="submit" />
        </div>
    }

    @using (Html.BeginForm("IncrementStatic", "Home"))
    {
        <div>
            Licznik kliknięć (zmienne static)
            <input value="Inkrementuj!" type="submit" />
            <div>@Model.StaticCounter</div>
        </div>
    }

    @*@using (Html.BeginForm("IncrementApplication", "Home"))
    {
        <div>
            Licznik kliknięć (zmienne Application)
            <input value="Inkrementuj!" type="submit" />
            <div>@Model.ApplicationCounter</div>
        </div>
    }

    @using (Html.BeginForm("IncrementSession", "Home"))
    {
        <div>
            Licznik kliknięć (sesja)
            <input value="Inkrementuj!" type="submit" />
            <div>@Model.SessionCounter</div>
        </div>
    }

    @using (Html.BeginForm("IncrementCookies", "Home"))
    {
        <div>
            Licznik kliknięć (cookie)
            <input value="Inkrementuj!" type="submit" />
            <div>@Model.CookieCounter</div>
        </div>
    }

    @using (Html.BeginForm("IncrementCache", "Home"))
    {
        <div>
            Licznik kliknięć (cache)
            <input value="Inkrementuj!" type="submit" />
            <div>@Model.CacheCounter</div>
        </div>
    }*@

</body>
</html>
